


<%= form_with(model: subscription, local: true, class: "meeting-form") do |form| %>

<% if subscription.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subscription.errors.count, "error") %> prohibited this subscription from being saved:</h2>

      <ul>
      <% subscription.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
   <!-- EXPLORE SECTION -->
  <section id="explore-section" class="bg-light text-muted py-5">
    <div class="container">
      <div class="row">
        <div class="col-md-6">

           <%= form.hidden_field :name, class: "form-control", placeholder: "First Name" %>


           <%= render "payment_form" %>

           <br>

  <div class="actions">
    <%= form.submit 'Checkout Now', class: 'btn btn-primary' %>



  </div>


        </div>
        <div class="col-md-6 ultimateguide">
          <br>
            <br>
          <h3>Ultimate Guide For Full-time Candidates</h3>

          <div class="d-flex">
            <div class="p-4 align-self-start">
              <i class="fas fa-check fa-2x"></i>
            </div>
            <div class="p-4 align-self-end">

            </div>
          </div>

          <div class="d-flex">
            <div class="p-4 align-self-start">
              <i class="fas fa-check fa-2x"></i>
            </div>
            <div class="p-4 align-self-end">

            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
<% end %>
